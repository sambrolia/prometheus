apiVersion: v1
kind: Service
metadata:
  name: pg-prometheus-service
  namespace: monitoring
spec:
  selector: 
    app: pg-prometheus
  type: NodePort  
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432